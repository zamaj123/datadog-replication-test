{
  "name": "@dd/ingestion",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "tsx src/server/start.ts",
    "build": "tsc -p tsconfig.json",
    "smoke:metrics": "node --import tsx src/scripts/metrics-roundtrip-smoke.ts",
    "test": "vitest run"
  },
  "dependencies": {
    "fastify": "^5.0.0",
    "json-bigint": "^1.0.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.8.0",
    "vitest": "^3.0.0"
  }
}
